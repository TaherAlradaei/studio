specVersion: "v1"
serviceId: "AlMaidan "
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "almaidan "
      cloudSql:
        instanceId: "almaidan-academy"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
